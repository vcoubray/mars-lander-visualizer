ktor {
    development = true
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ classes ]
    }
    application {
        modules = [ fr.vco.genetic.algorithm.visualizer.ApplicationKt.module ]
    }
}
